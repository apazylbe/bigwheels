apply plugin: 'com.android.application'
apply from: '../../gradle/common.gradle'

android {
    defaultConfig {
        applicationId "com.google.bigwheels.project_04_cube_xr"
        minSdk rootProject.ext.minSdk
        targetSdk rootProject.ext.targetSdk
        versionCode 1
        versionName "1.0"

        manifestPlaceholders = [
            sampleLibraryName : "vk_04_cube_xr",
            appLabel : "vk_04_cube_xr"
        ]

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        externalNativeBuild {
            cmake {
                arguments '-DPPX_ANDROID=TRUE',
                          '-DPPX_BUILD_TESTS=FALSE',
                          '-DBUILD_TESTS=FALSE', // Required for OpenXR
                          '-DPPX_BUILD_XR=TRUE',
                          '-DBUILD_LOADER=TRUE',
                          '-DBUILD_ALL_EXTENSIONS=TRUE'
                cppFlags '-std=c++20',
                         '-DXR_USE_PLATFORM_ANDROID'
                if (project.hasProperty('DXC_PATH')) {
                  arguments "-DDXC_PATH=$DXC_PATH"
                }
                targets "vk_04_cube_xr"
            }
        }
        multiDexEnabled true
        it.buildConfigField "String", "sample_library_name", '"vk_04_cube_xr"'
        sourceSets.main {
            manifest.srcFile '../../src/android/AndroidManifest.XR.xml'
        }
    }
}
